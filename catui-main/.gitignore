# godot stuff nobody wants in the repo, obviously
.godot/
.import/
export_presets.cfg

# imported assets are already effectively in .godot, so bye
# .import/

# junk generated by c# and visual studio (nobody versions this)
.mono/
.vscode/
.vs/
# *.csproj - keep this or godot explodes
# *.sln    - this one too
*.user

# c# build folders that just take up space
bin/
obj/

# temporary files or os nonsense cluttering things up
*.tmp
*.log
.DS_Store
Thumbs.db
desktop.ini

# where the magic happens (final builds)
exports/
builds/
*.pck
*.zip
*.apk
*.aab

# android (huge files, will be uploaded separately)
/android/
